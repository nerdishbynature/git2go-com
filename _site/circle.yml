general:
  branches:
    ignore:
        - gh-pages
  artifacts:
    - "build/Git2Go.ipa"
    - "build/Git2Go.dSYM.zip"
machine:
  xcode:
    version: "6.3.1"
dependencies:
  pre:
    - make ci_pre
test:
  override:
    - make ci_test
deployment:
  production:
    branch: production
    commands:
      - make deploy
